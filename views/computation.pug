extend layout
block content
    h1 #{title}
    p Math.abs() is applied to #{c1} is #{c}
    p Math.acos() is applied to #{c2} is #{h}
    p Math.sin () is applied to #{c3} is #{a}
    p Math.sinh () is applied to #{c4} is #{r}